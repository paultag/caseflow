%div#certifications-index(ng-controller="CertificationsIndexController" ng-init="index.init()")
  %h1 Start a New Certification
  :markdown
    Welcome to the Caseflow Certification and Dispatch Tool. Enter a case number to start the process.

  %hr

  %form(ng-submit="index.go_to_start()")
    .row
      .col.col-md-3
        %my-form-input(data-label="BFKEY" data-icon="info")
          %input(type="text" ng-model="index.id")

    .row
      .col.col-md-3
        .form-group
          %button.my-form-button(data-label="Start Certification" data-icon="chevron-right" data-color="primary" my-form-loading="index.loading")
